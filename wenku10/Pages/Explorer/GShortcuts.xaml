<Page
    x:Class="wenku10.Pages.Explorer.GShortcuts"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:wenku10.Pages.Explorer"
    xmlns:ex="using:wenku10.Pages.Explorer.Widgets"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="{StaticResource MajorBackgroundBrush}">
        <ScrollViewer x:Name="MainSV">
            <ItemsControl x:Name="MainContents">
                <ItemsControl.ItemContainerTransitions>
                    <TransitionCollection>
                        <EntranceThemeTransition IsStaggeringEnabled="True" FromVerticalOffset="100" />
                    </TransitionCollection>
                </ItemsControl.ItemContainerTransitions>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
    <Page.Resources>
        <DataTemplate x:Key="HorzThumbnailList">
            <ex:ThumbnailList Margin="10" Title="{Binding Name}" ItemsSource="{Binding Table.Items}" />
        </DataTemplate>
        <DataTemplate x:Key="Banner">
            <ex:MainBanner Title="{Binding Name}" ItemsSource="{Binding Table.Items}" RefSV="{Binding ElementName=MainSV}"/>
        </DataTemplate>
    </Page.Resources>
</Page>